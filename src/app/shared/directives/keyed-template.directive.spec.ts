import { KeyedTemplateDirective } from './keyed-template.directive';

describe('KeyedTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyedTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
